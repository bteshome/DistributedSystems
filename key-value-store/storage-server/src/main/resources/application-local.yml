server:
  port: '9409'

spring:
  application:
    name: keyvaluestore
  cloud:
    config:
      import-check:
        enabled: 'false'

storage-server:
  node:
    id: '1'
    #host: 0.0.0.0
    host: 127.0.0.1
    port: ${server.port}
    jmxPort: ${management.server.port}
    rack: ""
    storage-dir: /tmp/storage-server-${storage-server.node.id}
metadata-server:
  peers:
    - id: '1'
      #host: 0.0.0.0
      host: 127.0.0.1
      #host: 172.23.119.7
      port: '9311'



info:
  app:
    name: 'Key Value Store - Controller Node 1'
    version: '1.0'
    description: 'Key Value Store - Controller Node 1'

management:
  server:
    port: '0'
    #'9509'

#spring.config.import=configserver:http://localhost:8071/
