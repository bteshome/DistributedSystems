server:
  port: '9330'

spring:
  application:
    name: keyvaluestore
  cloud:
    config:
      import-check:
        enabled: 'false'

metadata:
  node:
    id: '3'
    #host: 0.0.0.0
    #host: 127.0.0.1
    host: localhost
    port: '9313'
  peers:
    - id: ${metadata.node.id}
      host: ${metadata.node.host}
      port: ${metadata.node.port}
    - id: '1'
      #host: 0.0.0.0
      #host: 127.0.0.1
      host: localhost
      port: '9311'
    - id: '2'
      #host: 0.0.0.0
      #host: 127.0.0.1
      host: localhost
      port: '9312'
  storage-dir: /tmp/raft-server-3

info:
  app:
    name: 'Key Value Store - Controller Node 3'
    version: '1.0'
    description: 'Key Value Store - Controller Node 3'

management:
  server:
    port: '9338'