{{- if .Values.kubeconfig.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "kube-entity-metrics.fullname" . }}-kubeconfig
  namespace: {{ template "kube-entity-metrics.namespace" . }}
  labels:
    {{- include "kube-entity-metrics.labels" . | indent 4 }}
type: Opaque
data:
  config: '{{ .Values.kubeconfig.secret }}'
{{- end -}}
