{{- if .Values.customResourceState.enabled}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "kube-entity-metrics.fullname" . }}-customresourcestate-config
  namespace: {{ template "kube-entity-metrics.namespace" . }}
  labels:
    {{- include "kube-entity-metrics.labels" . | indent 4 }}
  {{- if .Values.annotations }}
  annotations:
    {{ toYaml .Values.annotations | nindent 4 }}
  {{- end }}
data:
  config.yaml: |
    {{- toYaml .Values.customResourceState.config | nindent 4 }}
{{- end }}
